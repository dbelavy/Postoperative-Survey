<tr id= "question_group_<%= question_group.id %>">
  <td>
    <% if can_administer? %>
      <%= link_to question_group.name, question_group_questions_path(question_group)%>
    <% else %>
      <%= question_group.name %>
    <% end %>
  </td>
  <td>
    <ul class="horizontal-list">
      <p><%= link_to "Answer Questions", new_question_group_answer_group_path(question_group) %></p>
      <% if can_administer? %>
        <p><%= link_to "Results", [:results, question_group] %></p>
        <p><%= link_to "Delete", [question_group], method: :delete %></p>
      <% end %>
    </ul>
  </td>
</tr>
