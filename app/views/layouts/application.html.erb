<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Postoperative survey" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>



<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><a href="<%=main_app.root_path %>">Postop</a></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
     
        <% if user_signed_in? %>
         <li class="active"> <%= link_to "Patients", main_app.patients_path %> </li>
         <li class="active"> <%= link_to "Questionnaires", main_app.questionnaires_path%> </li>
         <li class="active"> <%= link_to "Sign out", main_app.destroy_user_session_path, :method => :delete %> </li>
        <% else %>
         <li class="active"> <%= link_to "Sign in", main_app.new_user_session_path %> </li>
        <% end %>

      
     </ul>

    <!-- Left Nav Section -->

  </section>
</nav>







  <body>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>